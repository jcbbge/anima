# Anima V1 Environment Configuration

# API Server
PORT=7100

# Database
POSTGRES_HOST=localhost
POSTGRES_PORT=7101
POSTGRES_DB=anima
POSTGRES_USER=anima
POSTGRES_PASSWORD=anima_dev_password

# Node Environment
NODE_ENV=development

# Logging
LOG_LEVEL=info

# Substrate Encoder Configuration
# Primary encoder: 'local' for locally-hosted models, 'remote' for API-based services
CORE_EMBEDDING_PROVIDER=local
CORE_ENCODER_URL=http://localhost:7102
CORE_EMBEDDING_MODEL=nomic-embed-text

# Secondary Encoder (Optional Fallback)
# Only required if CORE_EMBEDDING_PROVIDER=remote or for fallback scenarios
# FALLBACK_EMBEDDING_PROVIDER=remote
# SECONDARY_ENCODER_KEY=your-api-key-here
# FALLBACK_EMBEDDING_MODEL=text-embedding-3-small

# Technical Notes:
# - PORT 7100: API server
# - PORT 7101: PostgreSQL (internal 5432)
# - PORT 7102: Local encoder service (internal 11434)
# - Embedding dimensions: 768 (configurable via model)
# - Database: PostgreSQL 16 + pgvector 0.8.1
# - The system is substrate-agnostic; all vendor-specific configuration
#   is abstracted through the provider layer
