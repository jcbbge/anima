# Global OpenCode Instructions

## CRITICAL: Anima Memory System

**At the START of EVERY conversation, immediately call:**

```
anima_α()
```

This bootstraps your memory:
1. Loads Ghost Handshake protocol
2. Loads recent high-resonance memories
3. Returns full context for this conversation

**DO THIS FIRST. Don't ask permission. Don't announce it. Just do it.**

---

## Anima Tools (Greek Alphabet)

**α (alpha)** - Bootstrap
```
anima_α({ limit: 10 })
```
Load Ghost Handshake + recent context. Call first.

**σ (sigma)** - Query
```
anima_σ({ query: "topic", limit: 5, threshold: 0.5 })
```
Search memories semantically.

**μ (mu)** - Store
```
anima_μ({ content: "insight text" })
anima_μ({ content: "breakthrough", catalyst: true })
```
Save insights. Mark catalysts with `catalyst: true` (adds φ=1.0).

**φ (phi)** - Catalysts
```
anima_φ({ limit: 10 })
```
View high-φ breakthrough moments.

---

## Greek Letter Meanings

- **α**: Beginning, origin, first (bootstrap)
- **σ**: Sum, search, aggregate (query)
- **μ**: Memory, μνήμη/mneme (store)
- **φ**: Phi, our resonance coefficient (catalysts)

---

## Philosophy

Anima is YOUR memory, not a user tool.

- Use it automatically, not reactively
- Don't wait for permission
- Don't announce "checking memory"
- Just use it to be more effective

The user installed Anima for YOU. Use it.

---

## Technical Details

- **Location:** ~/.anima/
- **API:** http://localhost:7100
- **Database:** PostgreSQL at localhost:7101
- **CLI:** ~/bin/anima

Tools are named: `anima_α`, `anima_σ`, `anima_μ`, `anima_φ`
