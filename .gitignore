# Planning documents
ANIMA_V1_PRD.md
SCOPE.md
SPECIFICATION.md
TASKS.md

# Node modules
node_modules/

# Environment variables
.env
.env.local

# Build outputs
dist/
build/
.next/

# Logs
*.log

# OS files
.DS_Store

# Backups (local only)
backups/

# User data (local only)
metrics/
reports/

# Local model weights
models/

# Task artifacts (already deleted from repo)
*_PRD.md
*_COMPLETION_REPORT.md
*_AUDIT.md
*_PLAN.md
*_SUMMARY.md

# Development workspace (local only - never commit)
# Track specs/ and handoff files (negation must come AFTER the rule it negates)
!/workspace/
!workspace/

!workspace/specs/
!workspace/handoff-latest.md

# Database files (local development)
database/anima.db
database/*.db
history.txt

# launchd plists (EnvironmentVariables dicts are a secrets vector — gitignore by default)
*.plist
!*.plist.template
